<div class="sticky-one" id="navbar">
    <div id="logo-circle" class="position-absolute disappear">
        <a href="{{ path('home') }}">
            <img src="{{ asset('build/images/logo_circle.png') }}" alt="Logo de l'entreprise sans le texte">
        Angelus Cosy IMMO
        </a>
    </div>
    <div class="my_navbar w-100 text-center">
        <a href="{{ path('home') }}">
            <img id="logo_menu" class='img is-not-reduce' src="{{ asset('build/images/logo.png') }}" alt="logo de la société Angelus Cosy Immo">
        </a>
    </div>
    <div class="menu d-flex flew-row justify-content-center">
            <div class="link"><a href="{{ path('home') }}"{{ app.request.pathInfo == '/' ? ' class="focus"' : '' }}>Accueil</a></div>
            <div class="link"><a href="{{ path('projects') }}"{{ app.request.pathInfo == '/projects' ? ' class="focus"' : '' }}>Nos biens</a></div>
            <div class="link"><a href="{{ path('contact') }}"{{ app.request.pathInfo == '/contact' ? ' class="focus"' : '' }}>Contact</a></div>
    </div>
</div>
